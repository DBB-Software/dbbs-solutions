const testIdPrefix = 'login-wrapper'

export const LOGIN_WRAPPER_TEST_IDS = {
  TITLE: `${testIdPrefix}-title`,
  LOGOUT_BUTTON: `${testIdPrefix}-logout-button`,
  USER_EMAIL: `${testIdPrefix}-user-email`
}
