/**
 * Provides the configuration settings related to API log transports.
 * @module config
 */
export default {
  /**
   * Indicates whether API log transports are enabled.
   * This is used to configure logging transport across the API.
   * @type {boolean}
   * @default false
   */
  API_LOG_TRANSPORTS: false
}
