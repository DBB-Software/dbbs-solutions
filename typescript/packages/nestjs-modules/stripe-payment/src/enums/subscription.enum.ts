export enum SubscriptionStatus {
  TRIALING = 'trialing',
  ACTIVE = 'active',
  CANCELED = 'canceled',
  PAUSED = 'paused'
}

export enum SubscriptionStatusId {
  TRIALING = 1,
  ACTIVE = 2,
  CANCELED = 3,
  PAUSED = 4
}
