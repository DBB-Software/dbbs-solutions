import { Tab as MUITab, Tabs as MUITabs, TabProps, TabsProps } from '../..'

const Tab = (props: TabProps) => <MUITab {...props} />

const Tabs = (props: TabsProps) => <MUITabs {...props} />

export { Tab, Tabs }
