{
  "name": "@dbbs/cypress",
  "version": "1.0.0",
  "private": true,
  "description": "Cypress helper functions",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "clean": "npx rimraf .turbo node_modules dist bin *.tsbuildinfo",
    "lint": "eslint . --ext ts --max-warnings 0"
  }
}