import { render, screen } from '@testing-library/react'
import App from './App'

describe('<App />', () => {
  it('should render properly', async () => {
    render(<App />)
    const element = await screen.findByText('Sample APP')
    expect(element).toBeVisible()
  })
})
