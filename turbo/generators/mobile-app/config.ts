export enum AppTypes {
  REACT_NATIVE_CLI = 'react-native-cli',
  EXPO = 'expo'
}

export enum Storages {
  REDUX = 'redux',
  JOTAI = 'jotai',
  NONE = 'none'
}

// This value should be updated every time the react-native version is updated
export const SUPPORTED_RN_VERSION = '0.75.4'
