# IOS
app_identifier(ENV["{{ appNameUpperCase }}_APPLE_APP_IDENTIFIER"]) # The bundle identifier of your app
team_id(ENV["{{ appNameUpperCase }}_TEAM_ID"]) # Developer Portal Team ID
team_name(ENV["{{ appNameUpperCase }}_TEAM_NAME"]) # Developer Portal Team Name
# android
json_key_file("fastlane/google-play-sa.json")
package_name(ENV["{{ appNameUpperCase }}_ANDROID_APP_IDENTIFIER"])
