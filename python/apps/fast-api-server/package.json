{
  "name": "@dbbs/fast-api-server",
  "version": "1.0.0",
  "description": "DBBs FastAPI Server",
  "type": "module",
  "main": "../fast-api-server/src/main.py",
  "private": true,
  "scripts": {
    "dev": "poetry run python -m src",
    "lint": "poetry run ruff check .",
    "test": "poetry run coverage run -m pytest && poetry run coverage xml",
    "build": "echo \"no build specified\" && exit 0",
    "install:deps": "poetry install",
    "check-types": "poetry run mypy --strict ."
  }
}
